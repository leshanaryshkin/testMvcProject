IF OBJECT_ID(N'[__EFMigrationsHistory]') IS NULL
BEGIN
    CREATE TABLE [__EFMigrationsHistory] (
        [MigrationId] nvarchar(150) NOT NULL,
        [ProductVersion] nvarchar(32) NOT NULL,
        CONSTRAINT [PK___EFMigrationsHistory] PRIMARY KEY ([MigrationId])
    );
END;
GO

BEGIN TRANSACTION;
GO

CREATE TABLE [Users] (
    [ID] int NOT NULL IDENTITY,
    [Name] nvarchar(50) NULL,
    [Adress] nvarchar(50) NULL,
    [telephone] nvarchar(15) NULL,
    CONSTRAINT [PK_Users] PRIMARY KEY ([ID])
);
GO

CREATE TABLE [UsersLoginsPasswords] (
    [ID] int NOT NULL,
    [Login] nvarchar(15) NULL,
    [Password] nvarchar(50) NULL,
    [Is_admin] bit NOT NULL,
    CONSTRAINT [PK_UsersLoginsPasswords] PRIMARY KEY ([ID]),
    CONSTRAINT [FK_UsersLoginsPasswords_Users_ID] FOREIGN KEY ([ID]) REFERENCES [Users] ([ID]) ON DELETE CASCADE
);
GO

INSERT INTO [__EFMigrationsHistory] ([MigrationId], [ProductVersion])
VALUES (N'20220525165239_InitialCreate', N'5.0.17');
GO

COMMIT;
GO

BEGIN TRANSACTION;
GO

CREATE TABLE [Furnitures] (
    [ID] int NOT NULL IDENTITY,
    [Name] nvarchar(40) NULL,
    [costPrice] int NOT NULL,
    [pricePerOnce] int NOT NULL,
    [onStock] int NOT NULL,
    [isActualPosition] bit NOT NULL,
    CONSTRAINT [PK_Furnitures] PRIMARY KEY ([ID])
);
GO

CREATE UNIQUE INDEX [IX_Furnitures_Name] ON [Furnitures] ([Name]) WHERE [Name] IS NOT NULL;
GO

INSERT INTO [__EFMigrationsHistory] ([MigrationId], [ProductVersion])
VALUES (N'20220525171656_InitialTableFurniture', N'5.0.17');
GO

COMMIT;
GO

BEGIN TRANSACTION;
GO

CREATE TABLE [Profiles] (
    [ID] int NOT NULL IDENTITY,
    [Name] nvarchar(40) NULL,
    [costPrice] int NOT NULL,
    [pricePerMeter] int NOT NULL,
    [onStock] int NOT NULL,
    [isActualPosition] bit NOT NULL,
    CONSTRAINT [PK_Profiles] PRIMARY KEY ([ID])
);
GO

CREATE UNIQUE INDEX [IX_Profiles_Name] ON [Profiles] ([Name]) WHERE [Name] IS NOT NULL;
GO

INSERT INTO [__EFMigrationsHistory] ([MigrationId], [ProductVersion])
VALUES (N'20220525172737_InitialTableProfile', N'5.0.17');
GO

COMMIT;
GO

BEGIN TRANSACTION;
GO

CREATE TABLE [Balances] (
    [ID] int NOT NULL IDENTITY,
    [currencyName] nvarchar(20) NULL,
    [currencyBalance] int NOT NULL,
    CONSTRAINT [PK_Balances] PRIMARY KEY ([ID])
);
GO

CREATE UNIQUE INDEX [IX_Users_telephone] ON [Users] ([telephone]) WHERE [telephone] IS NOT NULL;
GO

CREATE UNIQUE INDEX [IX_Balances_currencyName] ON [Balances] ([currencyName]) WHERE [currencyName] IS NOT NULL;
GO

INSERT INTO [__EFMigrationsHistory] ([MigrationId], [ProductVersion])
VALUES (N'20220525191430_InitialTableBalance', N'5.0.17');
GO

COMMIT;
GO

